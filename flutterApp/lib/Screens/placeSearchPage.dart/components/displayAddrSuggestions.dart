import 'package:flutter/material.dart';

class DisplayAddrSuggetions extends StatelessWidget {
  const DisplayAddrSuggetions({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Container(),
    );
  }
}